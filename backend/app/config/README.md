# Config

This package is for reading environment variables and turning them into a
single `Settings` object that the rest of the app can use.

After implementation of `services/auth_service` sensitive information as JWT-sercret can be found in `settings/config`. It should be changed before usage in production.
