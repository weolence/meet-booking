# Tests

Use `unit/` for service-level and repository checks.

Use `integration/` for API flows through FastAPI's test client.

Coverage:

- login
- availability lookup
- successful booking
- duplicate booking conflict
- cancelling own booking
- admin cancellation
